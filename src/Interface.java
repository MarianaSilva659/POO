public interface Interface {
    String novoUtilizador(String email, String nome, String morada, int nif);
    int novoTransporte(double peq, double med, double gra, int margem, boolean aceitaPremium, int id);
}
